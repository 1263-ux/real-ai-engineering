# 备份与恢复说明

记录日期：`YYYY-MM-DD`

## 备份清单

| 对象 | 备份位置 | 生成时间 | 验证方式 | 保留策略 |
|---|---|---|---|---|
| `<配置/注册表/入口>` | `<路径>` | `<时间>` | `<方式>` | `<策略>` |

## 环境变量恢复

- 备份文件：`<路径>`
- 恢复前检查：`<填写>`
- 恢复命令或界面：`<填写>`
- 恢复后验证：`<填写>`

## Docker 恢复原则

- 使用产品原生备份、导入或迁移流程。
- 不直接替换 Volume/VHDX 文件。
- 恢复后核对容器、镜像、Volume 和 Compose 项目。

## WSL 恢复原则

- 使用发行版导出包或产品支持的迁移方式。
- 不直接删除或覆盖 VHDX。
- 恢复后核对发行版登记、用户数据和项目路径。

## VMware 恢复原则

- 使用 VMX/VMDK 的完整备份或产品支持的复制流程。
- 恢复后确认磁盘链、快照和虚拟机配置完整。

## 恢复验收

- `<应用启动>`
- `<命令入口>`
- `<项目构建或打开>`
- `<数据可读>`
- `<回滚结果>`
