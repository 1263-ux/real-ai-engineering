# 开发环境说明

记录日期：`YYYY-MM-DD`

## 当前基线

| 类别 | 工具 | 版本 | 实际路径 | 验证命令 | 状态 |
|---|---|---|---|---|---|
| Java | `<填写>` | `<填写>` | `<路径>` | `java -version` | `<填写>` |
| Node | `<填写>` | `<填写>` | `<路径>` | `node --version` | `<填写>` |
| Python | `<填写>` | `<填写>` | `<路径>` | `python --version` | `<填写>` |
| Git | `<填写>` | `<填写>` | `<路径>` | `git --version` | `<填写>` |

## 关键环境变量

| 变量 | 当前值/路径 | 作用 | 用户级/系统级 | 验证方式 |
|---|---|---|---|---|
| `<变量名>` | `<路径或仅记录存在>` | `<填写>` | `<用户 / 系统>` | `<命令>` |

不要在本文件记录密码、令牌、私钥或完整凭据，只记录变量名和是否存在。

## 缓存与仓库

| 工具 | 实际缓存/仓库路径 | 期望路径 | 是否已验证 | 备注 |
|---|---|---|---|---|
| `<npm / pnpm / uv / pip / Maven / Gradle>` | `<路径>` | `<路径>` | `<是 / 否>` | `<填写>` |

## PATH 核对

- 用户 PATH：`<有效项和无效项>`
- 系统 PATH：`<有效项和无效项>`
- 关键入口冲突：`<无 / 填写>`

## 使用原则

- 优先使用应用支持的配置方式迁移缓存，不用文件系统技巧替代配置。
- 迁移前备份配置，迁移后用新进程和真实命令验证。
- 旧缓存、旧版本和旧 PATH 项只列为候选，不自动删除。
