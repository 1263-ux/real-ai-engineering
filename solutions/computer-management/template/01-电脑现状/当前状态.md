# 当前状态

记录日期：`YYYY-MM-DD`

## 硬件与系统

| 项目 | 当前情况 |
|---|---|
| 电脑型号 | `<填写>` |
| 操作系统 | `<填写>` |
| Windows 版本/构建 | `<填写>` |
| BIOS/UEFI 版本 | `<填写>` |
| 处理器 | `<填写>` |
| 内存 | `<填写>` |
| 显卡 | `<填写>` |
| Secure Boot | `<已启用 / 已禁用 / 未验证>` |
| TPM | `<存在且就绪 / 未验证>` |

## 磁盘概况

| 盘符 | 总容量 | 可用空间 | 空闲率 | 备注 |
|---|---:|---:|---:|---|
| `<C:>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| `<D:>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |

## 当前环境评价

- 总体状态：`<正常 / 需注意 / 需处理 / 检查未完成>`
- 主要用途：`<填写>`
- 当前阻塞项：`<无 / 填写>`
- 不得自动处理的资产：`<填写>`

## 变化摘要

相对上次记录：

- `<变化一>`
- `<变化二>`

## 当前异常

- `<异常、证据、影响和后续决定>`

## 稳定性结论

`<填写已验证结论，以及仍需观察的事项。>`
