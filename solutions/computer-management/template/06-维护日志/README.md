# 维护日志

本目录只保存维护记录、周期巡检报告和测试结果，不保存密码、令牌、私钥或完整环境变量值。

## 使用规则

- 每次变更前记录目标、范围、风险、备份和回滚方式。
- 每次变更后记录实际结果和验证证据。
- 周期巡检只写报告、原始 JSON 和索引；不得自动修改长期状态。
- 对外分发时删除本目录下所有实际日志、报告、JSON 和测试输出，只保留模板。

## 模板

- `维护记录模板.md`
- `周期巡检\README.md`
