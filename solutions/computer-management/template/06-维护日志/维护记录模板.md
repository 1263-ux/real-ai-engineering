# YYYY-MM-DD 维护记录

## 目的

- 目标：`<填写>`
- 范围：`<填写>`
- 排除项：`<填写>`

## 操作前状态

- 现状证据：`<路径、命令输出或截图位置>`
- 风险等级：`<低 / 中 / 高 / 关键>`
- 备份位置：`<填写>`
- 回滚方法：`<填写>`

## 实际操作

- `<时间>：<操作及精确目标>`

## 验证结果

- `<验证命令、真实用户入口或数据检查>`
- 结果：`<通过 / 部分通过 / 失败>`

## 回滚方法

- `<精确恢复步骤>`

## 后续事项

- `<待观察、待用户确认或未完成项>`
