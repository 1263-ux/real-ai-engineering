# FROZEN — 已冻结基线

## dev-loop v3.5

> v3.5 是对 v3.2–v3.4.3 真实反馈的结构性整合：不新增流程，去重规范来源，公开方法论与本地资源配置分离。

## 冻结内容

- 核心方法：Owner 门禁、单 writer、工件交接、证据优先、Failure Routing、够用即停。
- 基础分级：L0 / L1 / L2 / L3；风险优先于文件数量。
- 路径修饰器：复杂、高风险、架构不确定、运维/发布；只改变必要门禁，不叠加第二套流程。
- 独立检查只有一个规范来源：单次、场景触发、fresh context、默认低成本；安全/数据/授权硬阻塞不能被用户加速信号覆盖。
- 停止规则：根因先行、时间盒、证据够了就停、已授权 PR 追加小修不重套 L3。
- 子 agent 默认后台并行，仅安全泄露、真实数据破坏或下一步依赖其答案时阻塞主线。
- FROZEN 仅在源码/依赖、运行环境、验收数据、核心闭环和用户证据未变化时有效。

## 整合边界

- 主 SKILL.md 保留可直接执行的原则；平台细节放 `adapters/dsh/` references。
- 公开模型资源 reference 只保留抽象角色；具体 Provider、价格、额度、路由和密钥不进入公开仓库。
- 以后新增规则前，先判断能否合并到现有四条执行规则；不能证明新增价值则不加。

## 后续变更

先出 DESIGN → Owner G1 → 实现与验证 → 更新本文件。禁止仅因一次事故追加新的全局硬规则。
