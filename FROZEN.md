# FROZEN — 已冻结基线

## dev-loop v3.4.2（2026-08-19 冻结）

> 基线来源：v3.1 → 真实会话复盘（零装载 / 零工件 / 环境失败误路由）→
> `solutions/dev-loop/v3.2-change-design.md`（G1 批准后实施并冻结）；
> v3.2.1 = 模型经济学落地（资源组配表 + C5 收窄）；
> v3.2.2 = 中转站 GPT 模型入表（deepseek 相对变贵，DSH 执行/编排优先走便宜档）；
> v3.3 = dsh-oks 真实项目复盘（门禁 / 高危运维 / 跨层链路 / 安装态补丁纪律，独立审查收敛）；
> v3.4 = dsh-oks/3080 运行态任务复盘（分级执行 L0–L3 + 去仪式化）；
> v3.4.1 = 收紧五处（风险优先于文件数 / 时间盒强制超时输出 / review 门禁位置 / 授权范围五要素 / FROZEN 失效条件）；
> v3.4.2 = PR 真实复盘（证据够了就停 / 独立检查单次场景触发 / 不重跑已通过检查 / 用户停止信号优先）。

### v3.2 相对 v3.1 的变更

- **C1 触发条件**：新增「触发条件」段——多文件改动 / 多步骤 git / 发布 / review / 工具链迁移 / 需验收的目标，第一步必须装载本 Skill 并播种 `.agent/CURRENT_STATE`（一句话即可）；违规信号 = 无 CURRENT_STATE。
- **C2 运维轻路径**：Playbook 新增「运维/发布」行——`PLAN(清单) → G2(大范围时) → 执行 → RESULT`，免 DESIGN、免形式化 AC；验证 = 真实运行结果 / diff / git 状态。
- **C3 环境失败归类**：Failure Routing 新增「环境/外部失败（网络/代理/第三方 500/认证/配额/服务抽风）→ BLOCKED」——独立计数，不触发 RETHINK；固定排障序：查代理/端口/环境 → 换源/镜像 → 退避重试（设上限）→ BLOCKED 上报。
- **C4 工件门槛**：所有任务最低一句话级工件——多步任务播种并更新 `.agent/CURRENT_STATE`（最小一行：状态 + 下一步），完成留最小 `RESULT`（结论 + 残留）。
- **C5 review 强制**：高风险 review 必须至少一次独立检查（防自审），默认便宜/白嫖档；升级稀缺专家需 G3。

### v3.2.1 增量（相对 v3.2）

- **C6 模型资源组配**：新增「模型资源组配（用户资产）」段——Codex Pro（主力，订阅已付）、DeepSeek API（编排/轻活）、Codex 网页版（高判断 review）、智谱 GLM-4.5-Air / GLM-4.1-Thinking（白嫖额度，高判断优先）、便宜低端 key（token 重活）、DSH（只编排不默认全包）。密钥不入库，存环境变量/配置。
- **C5 收窄**：高风险 review 从「必须至少一次独立 Expert」改为「必须一次独立**检查**，默认便宜/白嫖档；只有检查发现问题或方案高危才升级稀缺专家（需 G3）」——防的是自审，不是上贵的。

### v3.2.2 增量（相对 v3.2.1）

- **C7 中转站模型入表**：资源组配表新增「GPT 中转站（openai-completions）」——gpt-5.6 / gpt-5.6-sol / gpt-5.6-terra / codex-auto-review / gpt-5.5 / gpt-5.4 / gpt-image-2，能力档高，官方倍率 0.06（预算按 0.1 计）。DeepSeek 因相对变贵降为兜底；DSH 执行/编排 token 优先走中转站；独立检查默认 codex-auto-review（便宜档）。密钥不入库。

### v3.3 增量（相对 v3.2.2，dsh-oks 事故 → 独立审查收敛后冻结）

> 初版六条经 fresh-context 独立审查判定为"把单次事故经验硬编码成全局强制流程"，按审查建议收敛后冻结：
> 主 Skill 只留三类短原则；DSH 具体链路 / 操作单 / troubleshooting 下沉到 `adapters/dsh/` reference。

- **原则 1 · 范围**：实质性范围扩张（显著扩大 blast radius / 权限边界 / 数据影响 / 发布范围）时，重新确认目标与执行风险（可复用 G1/G2）；不要求所有跨系统改动重建全套工件。
- **原则 2 · 运维与临时补丁**：高危动作须有验证与回退，或明确记录不可回退及理由；临时/安装态补丁至少记录 临时原因 + 验证方式 + 回退或上游跟踪。
- **原则 3 · 跨层**：跨层故障按依赖链逐跳验证；独立检查前置仅用于链路假设不确定、平台影响大或风险高的情况。
- **下沉 reference**：`adapters/dsh/web-plugin-exposure-chain.md`（settings 暴露链路 checklist）、受控操作单、Windows 路径 ENOENT troubleshooting 存 reference，主 Skill 只引用。

### v3.4 增量（相对 v3.3，来自 dsh-oks/3080 运行态任务复盘）

- **C8 分级执行 L0–L3**：小任务不套完整流程——L0 一行修改（文案/CSS/解释）→ 改+验证+汇报；L1 普通代码（单模块/一两个文件）→ 简短计划+改+测试；L2 运行态/跨模块（插件/Host-Client/配置写回/重启/环境）→ 完整流程+独立检查+受控运行态验证；L3 外部交付（commit/push/PR/发布/外部消息）→ 完整流程+Owner 逐项授权。解决"小任务被大流程拖慢、流程完成感≠问题解决"。
- **时间盒**：根因判断 ≤15 分钟、可运行修复 ≤30 分钟；超时明确说出阻塞点，不静默续跑。
- **根因先行**：先给根因再给流程；根因明确后立即执行，不重复核查已确定的事实。
- **独立检查不阻塞**：review 与本地测试/准备并行，只有阻塞性结论才等待。
- **运行态授权提前**：重启服务/改真实数据/外部发布在计划阶段请求一次明确授权，不拖到源码流程跑完才发现。
- **工件每轮一次**：每轮只更新一次 .agent 文件，避免反复写造成额外开销；状态滞后时下轮开工先同步。

### v3.4.1 增量（相对 v3.4，收紧五处）

- **风险优先于文件数量**：涉及真实数据/服务重启/认证/权限/协议/外部发布，即使只改一个文件，最低按 L2/L3 执行。
- **时间盒强制输出**：超时必须停止试错并输出 已确认事实 / 当前假设 / 已排除项 / 阻塞原因 / 下一步所需决策。
- **review 门禁位置**：review 不得阻塞 IMPLEMENT 与本地 VERIFY；高风险任务 DONE 前必须 review PASS，除非 Owner 明确接受风险。
- **授权范围五要素**：运行态授权须含 具体动作 / 目标服务 / 影响范围 / 回滚方式 / 是否允许改真实数据。
- **FROZEN 失效条件**：源码/依赖变化、运行环境变化、验收数据变化、核心闭环失效、用户报告与证据矛盾 → 冻结失效，需重新验证。

### v3.4.2 增量（相对 v3.4.1，PR 真实复盘：去流程税）

- **独立检查单次、场景触发**：仅 易泄露数据 / 改真实配置 / 涉及路径权限发布包 / 用户明确要求 review 时启用，且只一次（默认便宜档）；其余场景以本地验证为准，不默认拉 reviewer。
- **证据够了就停（停止条件）**：用户说「直接 PR / 继续 / 授权 / 快」= 明确停止信号；已通过的检查不重复全量重跑；验证 = 针对性测试（+必要时 build/pack/扫描）+ 一次状态确认，够了即交付；不追加 review、不重复拉取、不创建展示性 agent。
- 修正 v3.4.1「高风险任务 DONE 前必须 review PASS」被误读为"每次推送前多轮审查"的歧义。

### 冻结规则

- 已冻结路径不重开、不重复全量验证（除非数据损坏 / 凭据泄漏 / 核心闭环失效）。
- 后续迭代：先出变更设计（DESIGN）→ 人工 G1 门禁 → 通过后冻结基线升至下一版本。
